package sud.engine.mechanics.behaviors.IA;

import sud.actors.Actor;
import sud.engine.mechanics.behaviors.Behavior;

public class SeekBehavior implements Behavior {
    //pensato per i nemici (è la versione dei nemici del patrol)
    @Override
    public void perform(Actor self) {

    }
}
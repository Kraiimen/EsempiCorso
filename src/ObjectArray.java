public class ObjectArray{
    public static void main(String[] args){
        String[] as = new String[5];
        // stampare la prima stringa maiuscola
        // System.out.println(as[0].toUpperCase());
        String[] students = {"Vittorio,griffindor" , "Lorenzo,ravenclaw" , "Carmine"};
        System.out.println(students[0].toUpperCase());

        //dichiarare la stringa casate 
        String[] griffindor = new String[6]; // creo una array di stringhe per la casata di griffindor
        int grCounter = 0; // conta studenti nella casata 
        griffindor[grCounter] = "Emanuele"; // mette lo studente dentro l'array e nel counter
        grCounter ++; // a ogni studente aumenta il conto di 1 
        String s1 = students[0];
        System.out.println(s1);

        String[] sTokens = s1.split(","); //funzione delle stringhe, spezzando la stringa 
        System.out.println(sTokens[0]);
        System.out.println(sTokens[1]);
        // nuova array stringhe che spezza la prima stringa dal caratte , quindi divide in due array 0 nome 1 casata 

        String x = "Lorenzo";
        if
        // in alternativa le stringhe messe randomiche nelle casate 
    }
}
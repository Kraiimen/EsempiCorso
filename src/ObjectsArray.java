public class ObjectsArray {
    public static void main(String[] args) {
        String[] as  = new String[5];
        // stampare maiuscola prima stringa
        // System.out.println(as[0].toUpperCase());
        String[] student = {"Carmine", "Federico", "Marta" , "Filippo"};
        System.out.println(student[1].toUpperCase());


        String[] griffindor = new String[5];
        int grCounter = 0; // variabile per contare gli studente
        griffindor[grCounter] = "Emanuele";
        grCounter
    }
}
